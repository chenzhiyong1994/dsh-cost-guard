# Screenshot assets / 截图素材

| File | Description |
| --- | --- |
| `hud-idle.png` | Real DSH conversation view with the idle Cost Guard HUD highlighted. |
| `settings-pricing.png` | Real Cost Guard settings view with pricing and per-model session totals. |

The two PNG screenshots were supplied by the project owner for public documentation and were visually reviewed before release. No API key, user name, local path, or private account identifier is visible.

The screenshots show an earlier configured instance with the scheduled multiplier enabled and custom rates. `v4.3.0` defaults to the official peak/off-peak rates with the schedule enabled; see the main README for details.

---

两张 PNG 是项目所有者提供的真实效果截图，发布前已检查可见像素；其中不含 API Key、用户名、本机路径或私密账号标识。

截图中的实例启用了时段倍率并保留了旧自定义价格。`v4.3.0` 默认使用官方峰谷价并开启峰谷计价，详见主 README。
