# 变更日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，版本遵循语义化版本。

## [Unreleased]

### 新增
- 中英双语项目主页：费用示例、真实截图切换、安装提示词复制、常见问题与响应式布局。
- GitHub Pages 自动部署工作流，并在中英 README 和项目元信息中补充主页入口。

## [4.3.1] - 2026-08-18

### 修正
- 修复设置页崩溃：会话累计的按模型汇总表误用 `s.hud` 引用（`settings` 词典下无 `hud` 子对象），导致 `Cannot read properties of undefined (reading 'in')`；改用独立的 `hudS = STRINGS[lang].hud`。

## [4.3.0] - 2026-08-18

### 新增
- **中英文界面**：底部横条与设置页跟随 DSH 通用设置的语言（中文 / English）自动切换（订阅客户端 `locale` 服务，设置页导航标签使用 label thunk 按投影重读）。
- 设置页与横条全部文案双语化，含峰谷规则、统计卡、备份区与错误提示。

### 修正
- 默认价恢复为官方 2026-08-17 生效的峰谷定价（¥/1M tokens，输入未命中 / 输出 / 输入缓存命中）：Flash 谷价 1.5 / 4.5 / 0.05，Pro 谷价 4.5 / 13.5 / 0.15；高峰 ×2（北京时间 9:00–12:00、14:00–18:00），峰谷默认开启。修正 v4.2.0 中错误的默认值 1 / 2 / 0.02 与 3 / 6 / 0.025。
- HUD 峰/谷徽标仅在峰谷开关开启时显示（新增 `peakEnabled` 判断）。

## [4.2.0] - 2026-08-18

### 变更
- 将默认 DeepSeek V4 人民币基础价更新为官方定价页在 2026-08-18 显示的价格：Flash 为 1 / 2 / 0.02，Pro 为 3 / 6 / 0.025（输入未命中 / 输出 / 输入缓存命中，¥ / 1M tokens）。
- 时段倍率改为默认关闭的可选自定义功能，不再声称 DSH 或 DeepSeek 定义了峰谷计价。
- 界面和文档将金额准确定位为“真实 token 用量 + 可配置单价”得到的本地估算，并提醒以供应商实际账单为准。
- 补充英文默认 README、中文 README、真实截图、封面、贡献与安全文档。

## [4.1.0] - 2026

### 变更
- 移除轮次结算卡（`conversation.chat.turnTail` Slot 注册及 TurnTailCard 组件）。
- 实际开销、模型调用次数、输入 Token（未命中）、输出 Token、缓存命中 Token、模型名称全部并入底部横条展示；空闲态显示「上次」明细与会话累计。
- Host 修正：轮次现在记录实际使用的模型名（`rec.model`），底部横条与按模型汇总统计可正确显示模型。

## [4.0.0] - 2026

### 移除
- **彻底移除预估/拦截/复盘能力**：删除执行前成本预估（`tokenMeter`）、`agent/pre-step` 确认门槛、GateOverlay 全屏确认弹窗、预估 vs 实际复盘与自适应校准，以及所有相关配置项（门槛、预估算法、复盘阈值、常驻监控选项）。预估效果不可靠，插件定位回归为**纯 token 计费监控**。
- 移除 `shell.overlay` Slot 注册、`pending.*` / `review.*` RPC、`agentDefaultModel` 依赖。

### 保留（v3.0 起的能力）
- 底部横条 HUD：执行中实时显示已花费金额、输入/输出/缓存命中 token、步数与峰谷徽标；空闲时显示上次实际花费与全会话累计。
- 轮次结算卡（turnTail）：仅显示基于真实用量的实际开销，不再有预估对比。
- 设置页：模型定价（官方三列 + 峰谷双窗口）、会话累计统计、配置导入/导出。
- 启动自愈 `replay()`：从会话日志重建历史轮次用量与结算。

## [3.0.0] - 2026

### 移除
- 移除左右吸附样式与「常驻监控位置」设置项，仅保留底部横条作为唯一常驻监控形态。

### 保留（自 v2.8 起）
- 底部横条：与输入框像素级等宽（复用 composer CSS 变量）、两端对齐布局、无百分比进度条、实时计费明细（已花费/预估 + 输入/输出/缓存命中 token + 步数 + 峰谷徽标）。

## [2.7.0] - 2026

### 新增
- 启动自愈：插件每次启动时从各会话持久日志（`session.events`）重建全部历史轮次——回溯重算预估、按事件时刻峰谷价回放实际开销、重跑结算与学习统计。解决插件更新/重启后预估丢失、复盘缺失、底部条清空问题。

## [2.6.0] - 2026

### 变更
- 定价表对齐 DeepSeek 官方三列：输入·未命中 / 输出 / 输入·命中；移除「缓存写」配置列，缓存写入 token 并入未命中输入价计费（官方无单独缓存写计费项）。

## [2.4.0] - 2026

### 修正
- 峰谷时段修正为两个窗口：北京时间 9:00–12:00、14:00–18:00 为高峰（单价 ×2），其余为空闲时段（高峰价的一半）；设置页支持双窗口编辑。

## [2.3.0] - 2026

### 变更
- 定价默认值切换为 DeepSeek V4 官方价（¥/1M）：v4-flash 1.5/4.5/0.05，v4-pro 4.5/13.5/0.15；新增旧模型名别名匹配（deepseek-chat→v4-flash 价、deepseek-reasoner→v4-pro 价）。
- 移除四角卡片样式（该形态体验不佳，后于 v2.8 以锚点定位方式回归、最终于 v3.0 移除）。

## [2.0.0] - 2026

### 新增
- 任务级成本模型：从消息提取复杂度特征（工具动词 / 文件引用 / 代码块 / 多步意图 / 附件），估算步数、工具结果增量与多步缓存命中折价；按模型自适应学习输出倍率与步数倍率。
- DeepSeek V4 峰谷计价支持。
- 品牌名由「花费守卫」更名为「开销监控」；设置页重构并修复开关视觉问题。
- 计价单位由 USD 切换为 CNY（¥），界面附美元换算。

## [1.0.0] - 2026

### 新增
- 首个可用版本：任务执行前预估花费（tokenMeter 度量 × 模型定价）并按门槛确认；执行中常驻「已花费 / 预估」监控条；完成后预估 vs 实际对比；偏差大时复盘并按 EMA 校准优化预估算法。
- 拦截确认卡片（全屏 overlay）、轮次结算卡（turnTail）、设置页。
